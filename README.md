# mTeX

**mTeX** -- A personal LaTeX toolkit for quickly creating and managing custom notes, reports, articles, and presentations.

## Setup

```bash
sudo ln -sf /home/malick/Desktop/projects/mtex/bin/mtex /usr/local/bin/mtex
```
